<UserControl
    x:Class="DirectCanvas.UI.Widgets.DCUIControl_Parameter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DirectCanvas.UI.Widgets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <TextBox x:Name="ParameterTextBox" Header="{x:Bind DCBrushParameter.Name,Mode=OneWay}" TextChanged="TextBox_TextChanged" ToolTipService.ToolTip="{x:Bind DCBrushParameter.Description,Mode=OneWay}" Visibility="{x:Bind DCBrushParameter.ViewTextBox,Mode=OneWay}"/>
        <Slider x:Name="ParameterSlider" Header="{x:Bind DCBrushParameter.Name,Mode=OneWay}" ValueChanged="ParameterSlider_ValueChanged" ToolTipService.ToolTip="{x:Bind DCBrushParameter.Description,Mode=OneWay}" Visibility="{x:Bind DCBrushParameter.ViewSlider,Mode=OneWay}"/>
    </Grid>
</UserControl>
